// Este arquivo existe apenas para compatibilidade. A configuração real está em next.config.js (CommonJS).
// Reexporta a configuração CJS para evitar duplicidade e comportamentos divergentes em ambientes diferentes (local vs Vercel).
import nextConfigCJS from './next.config.js';
export default nextConfigCJS;